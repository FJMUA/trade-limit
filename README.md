# trade-limit
Plugin that restricts villagers from generating high-value goods

## Commands (just for op)

```properties
/tLimit add            - add the player's mainhand as restricts item to configuration file
/tLimit delete <index> - delete restricted items stored in the configuration file.
/tLimit show           - show all items stored in the configuration file.
```